/**
 * proxy-stage.ts — etapa 5B del pipeline: transcode paralelo de los clips
 * 'leccion' a proxies de edición 1080p30, escritos en
 * jobs/<id>/assets/proxies/. STUB: la implementación real llega en
 * FLUJO-AGROMAX-8nl.3.
 */

/** Corre la etapa de generación de proxies (5B) para un job. Aún no implementado. */
export async function runProxyStage(_jobId: string): Promise<void> {
  throw new Error("pendiente");
}
