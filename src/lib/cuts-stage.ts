/**
 * cuts-stage.ts — etapa 5C del pipeline: cortes deterministas derivados de
 * los huecos entre segmentos de la transcripción de Whisper, escritos en
 * jobs/<id>/plan/cuts/<lessonId>.json. STUB: la implementación real llega
 * en FLUJO-AGROMAX-8nl.4.
 */

/** Corre la etapa de cálculo de cortes (5C) para un job. Aún no implementado. */
export async function runCutsStage(_jobId: string): Promise<void> {
  throw new Error("pendiente");
}
