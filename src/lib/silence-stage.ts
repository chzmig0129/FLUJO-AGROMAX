/**
 * silence-stage.ts — etapa 5A del pipeline: detección de silencio con
 * ffmpeg silencedetect sobre los clips 'leccion' de la estructura, para
 * escribir jobs/<id>/probe/silence.json. STUB: la implementación real llega
 * en FLUJO-AGROMAX-8nl.2.
 */

/** Corre la etapa de detección de silencio (5A) para un job. Aún no implementado. */
export async function runSilenceStage(_jobId: string): Promise<void> {
  throw new Error("pendiente");
}
